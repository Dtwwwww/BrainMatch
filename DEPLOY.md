# 智析 BrainMatch — 部署方案（Vercel + 阿里云 CDN）

> **方案特点**：零服务器成本、国内访问快、维护简单  
> **预估费用**：¥0–5/月（按 CDN 流量计费）  
> **适用条件**：域名已完成 ICP 备案（阿里云 CDN 国内加速必需）

---

## 一、前置条件

| 条件 | 状态 | 说明 |
|------|------|------|
| 域名已注册 | ✅ 已拥有 `dtwwww.cn` | 阿里云域名控制台管理 |
| ICP 备案 | ⏳ 备案审核中 | **备案通过后才能开启「中国内地」加速**；未备案期间可先部署到「全球（不含中国内地）」 |
| Vercel 项目 | ✅ 已部署 | 当前项目已部署在 Vercel，默认域名可用 |
| 环境变量 | ✅ 已配置 | Supabase / Upstash / OpenAI 等密钥已在 Vercel 配置 |

---

## 二、部署架构

```
用户访问 dtwwww.cn
    ↓
阿里云 CDN（国内边缘节点）
    ↓
回源到 Vercel（海外源站）
    ↓
返回页面/API 响应
```

- **Vercel**：负责代码构建、Serverless API、自动 HTTPS、全球分发  
- **阿里云 CDN**：负责国内缓存加速，降低回源延迟

---

## 三、完整部署步骤

### 步骤 1：获取 Vercel 源站地址

1. 打开 [Vercel Dashboard](https://vercel.com/dashboard) → 进入 BrainMatch 项目  
2. 复制**默认 Vercel 域名**（如 `brain-match-xxx.vercel.app`）  
3. 确认项目运行正常：`https://brain-match-xxx.vercel.app` 能正常访问

> **不要直接用 dtwwww.cn 访问 Vercel**，后面要通过 CDN 统一接入。

---

### 步骤 2：在阿里云添加 CDN 加速域名

1. 打开 [阿里云 CDN 控制台](https://cdn.console.aliyun.com/) → **域名管理** → **添加域名**
2. 按以下配置填写：

| 配置项 | 填写值 |
|--------|--------|
| 加速域名 | `dtwwww.cn` |
| 业务类型 | **图片小文件** |
| 源站信息 | 源站域名 |
| 源站地址 | 你的 Vercel 默认域名（如 `brain-match-xxx.vercel.app`） |
| 端口 | **443**（HTTPS） |
| 加速区域 | **仅中国内地**（备案通过后选用）/ **全球（不含中国内地）**（备案前临时用） |

3. 点击 **确定**

---

### 步骤 3：验证域名归属权（首次添加需验证）

阿里云会要求验证你对 `dtwwww.cn` 的所有权：

1. 记录阿里云给出的 **TXT 验证记录**：
   - 主机记录：`verification`
   - 记录值：`verify_xxxxxxxxxxxxxxxx`（以页面显示为准）
2. 打开 [阿里云域名解析控制台](https://dns.console.aliyun.com/) → `dtwwww.cn` → **添加记录**：

| 记录类型 | 主机记录 | 记录值 |
|---------|---------|--------|
| TXT | `verification` | 阿里云给出的 verify_xxx 值 |

3. **等待 3–5 分钟**（DNS 生效需要时间）
4. 返回 CDN 页面，点击 **「点击验证」**
5. 验证通过后，阿里云会分配一个 **CNAME 地址**（如 `dtwwww.cn.w.kunlunar.com`）

---

### 步骤 4：配置域名解析（指向 CDN）

在阿里云域名解析控制台，为 `dtwwww.cn` 添加/修改记录：

| 记录类型 | 主机记录 | 记录值 | 说明 |
|---------|---------|--------|------|
| CNAME | `@` | 阿里云分配的 CNAME 地址 | 根域名指向 CDN |
| CNAME | `www` | 阿里云分配的 CNAME 地址 | www 子域名指向 CDN |

> 如果之前添加过 A 记录或 TXT 验证记录之外的记录，**删除旧的 A 记录**，避免冲突。

---

### 步骤 5：等待 CDN 生效

- CDN 状态变为 **「正常运行」** 通常需要 **5–15 分钟**
- 期间可刷新 CDN 控制台 → 域名列表查看状态

---

### 步骤 6：配置 HTTPS（SSL 证书）

1. CDN 控制台 → 域名管理 → 点击 `dtwwww.cn`
2. 左侧选择 **HTTPS 配置**
3. 打开 **HTTPS 安全加速**
4. 证书类型选择：**免费证书**（阿里云自动申请并续期 Let's Encrypt 证书）
5. **强制跳转**：打开「HTTP → HTTPS」
6. 保存配置

---

### 步骤 7：配置缓存规则（优化性能）

在 CDN 域名的 **缓存配置** 页面，添加/确认以下规则：

| 类型 | 路径 | 缓存时间 | 说明 |
|------|------|---------|------|
| 文件后缀 | `js,css,png,jpg,jpeg,gif,ico,svg,woff,woff2` | 30 天 | 静态资源长期缓存 |
| 目录 | `/` | 0 秒（不缓存）或 5 分钟 | HTML 页面建议不缓存或短缓存，避免更新不生效 |
| 全路径 | `/api/*` | 0 秒 | API 接口不缓存 |

> 缓存时间可根据实际需求调整。发布后若页面未更新，可在 CDN 控制台手动 **刷新缓存**。

---

### 步骤 8：可选优化配置

| 配置项 | 建议 | 位置 |
|--------|------|------|
| Gzip 压缩 | 开启 | 性能优化 |
| Brotli 压缩 | 开启 | 性能优化 |
| 智能压缩 | 开启 | 性能优化 |
| Referer 防盗链 | 可选开启 | 安全防护 |
| IP 黑白名单 | 按需配置 | 安全防护 |

---

## 四、验证部署成功

### 方法 1：浏览器访问
直接访问 `https://dtwwww.cn`，确认：
- 页面正常加载
- 地址栏显示 🔒 安全锁（HTTPS 生效）

### 方法 2：命令行验证
```bash
curl -I https://dtwwww.cn
```
查看响应头中是否包含 `X-Cache: HIT` 或阿里云 CDN 相关字段。

### 方法 3：对比速度
- 直接访问 Vercel 域名（海外线路）
- 访问 `dtwwww.cn`（CDN 加速后）
- 国内用户应该明显感觉到后者更快

---

## 五、环境变量清单

以下变量已在 Vercel 配置，无需在 CDN 侧重复配置：

| 变量名 | 来源 | 用途 |
|--------|------|------|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase 项目设置 | 数据库连接 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase 项目设置 | 前端匿名鉴权 |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase 项目设置 | 服务端管理权限 |
| `UPSTASH_REDIS_REST_URL` | Upstash 控制台 | 限流缓存 |
| `UPSTASH_REDIS_REST_TOKEN` | Upstash 控制台 | 限流缓存鉴权 |
| `OPENAI_API_KEY` | OpenAI 开发者平台 | AI 分析 |

> **注意**：环境变量只配置在 **Vercel 项目 → Settings → Environment Variables** 中，阿里云 CDN 只是加速层，不处理业务逻辑。

---

## 六、后续更新代码

本项目持续部署（CI/CD）由 Vercel 自动处理：

1. 本地修改代码
2. `git push` 到主分支
3. Vercel 自动构建并部署
4. CDN 会在下次用户访问时自动回源获取最新内容

如需立即让 CDN 刷新缓存（避免旧页面）：
- CDN 控制台 → `dtwwww.cn` → **刷新缓存** → 输入 `/*` 或指定 URL → **提交**

---

## 七、费用预估

| 流量 | CDN 费用（中国内地） | 说明 |
|------|---------------------|------|
| 0–10 GB/月 | ¥0–2.4 | 个人项目通常在此区间 |
| 10–50 GB/月 | ¥2.4–12 | 用户量增长后 |
| 50–100 GB/月 | ¥12–24 | 需关注缓存命中率 |

> Vercel 本身对个人免费项目不收费。总费用 ≈ CDN 流量费。

---

## 八、常见问题（FAQ）

### Q1：备案还没通过，能先部署吗？
**可以**。添加 CDN 域名时，加速区域选择 **「全球（不含中国内地）」**，这样海外用户可先访问。等备案通过后，修改为 **「仅中国内地」** 即可。

### Q2：访问 dtwwww.cn 显示 502/504？
- 检查 CDN 源站地址是否填对了 Vercel 域名
- 检查 Vercel 项目是否正常运行
- 检查 HTTPS 回源配置是否正确

### Q3：页面更新了但用户看到旧内容？
- CDN 缓存了旧页面，去 CDN 控制台手动 **刷新缓存**
- 或调短 HTML 文件的缓存时间

### Q4：API 请求被缓存了？
- 确保缓存配置中 `/api/*` 路径的缓存时间为 **0 秒**
- 检查 API 响应头中是否有 `Cache-Control: no-cache`

### Q5：HTTPS 证书报错？
- 确认 CDN 域名的 HTTPS 配置已开启
- 确认「强制跳转 HTTP→HTTPS」已打开
- 若使用自有证书，确保证书未过期且域名匹配

---

## 九、紧急回滚

若 CDN 配置出错导致网站不可用：

1. 临时绕过 CDN：将域名解析的 CNAME 改回 **A 记录**，直接指向 Vercel（虽然国内慢但可用）
2. 或在 CDN 控制台临时 **停用加速域名**
3. 修复配置后重新启用

---

## 十、部署检查清单（Deploy Checklist）

提交代码/发布前确认：

- [ ] Vercel 构建成功且无报错
- [ ] 环境变量已在 Vercel 配置完整
- [ ] `dtwwww.cn` 域名已添加 CDN 加速
- [ ] 域名解析 CNAME 指向 CDN
- [ ] CDN HTTPS 已开启且强制跳转
- [ ] API 路径缓存时间为 0
- [ ] 浏览器访问 `https://dtwwww.cn` 正常
- [ ] 备案状态已确认（国内加速必需）

---

*文档生成时间：2026-07-30*  
*适用项目：BrainMatch（智析）*  
*部署方式：Vercel 托管 + 阿里云 CDN 国内加速*
